[sqlfluff]
dialect = postgres
templater = raw
max_line_length = 200

[sqlfluff:rules:layout.long_lines]
max_line_length = 200

[sqlfluff:rules:references.keywords]
# Disable the rule that prevents using SQL keywords as identifiers
ignore_words = name,key,type,order,position,location,role,action
